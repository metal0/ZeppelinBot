diff a/backend/src/data/GuildSavedMessages.ts b/backend/src/data/GuildSavedMessages.ts	(rejected hunks)
@@ -49 +49 @@ export class GuildSavedMessages extends BaseGuildRepository {
-      data.attachments = Array.from(msg.attachments.values()).map((att) => ({
+      data.attachments = Array.from(msg.attachments.values()).map(att => ({
@@ -62 +62 @@ export class GuildSavedMessages extends BaseGuildRepository {
-      data.embeds = msg.embeds.map((embed) => ({
+      data.embeds = msg.embeds.map(embed => ({
@@ -69 +69 @@ export class GuildSavedMessages extends BaseGuildRepository {
-        fields: embed.fields.map((field) => ({
+        fields: embed.fields.map(field => ({
@@ -122 +122 @@ export class GuildSavedMessages extends BaseGuildRepository {
-      data.stickers = Array.from(msg.stickers.values()).map((sticker) => ({
+      data.stickers = Array.from(msg.stickers.values()).map(sticker => ({
