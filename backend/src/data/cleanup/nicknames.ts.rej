diff a/backend/src/data/cleanup/nicknames.ts b/backend/src/data/cleanup/nicknames.ts	(rejected hunks)
@@ -14 +14,4 @@ export async function cleanupNicknames(): Promise<number> {
-  const dateThreshold = moment.utc().subtract(NICKNAME_RETENTION_PERIOD, "ms").format(DBDateFormat);
+  const dateThreshold = moment
+    .utc()
+    .subtract(NICKNAME_RETENTION_PERIOD, "ms")
+    .format(DBDateFormat);
@@ -31 +34 @@ export async function cleanupNicknames(): Promise<number> {
-        id: In(rows.map((r) => r.id)),
+        id: In(rows.map(r => r.id)),
