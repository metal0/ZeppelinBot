diff a/backend/src/data/cleanup/usernames.ts b/backend/src/data/cleanup/usernames.ts	(rejected hunks)
@@ -14 +14,4 @@ export async function cleanupUsernames(): Promise<number> {
-  const dateThreshold = moment.utc().subtract(USERNAME_RETENTION_PERIOD, "ms").format(DBDateFormat);
+  const dateThreshold = moment
+    .utc()
+    .subtract(USERNAME_RETENTION_PERIOD, "ms")
+    .format(DBDateFormat);
@@ -31 +34 @@ export async function cleanupUsernames(): Promise<number> {
-        id: In(rows.map((r) => r.id)),
+        id: In(rows.map(r => r.id)),
