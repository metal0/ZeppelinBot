diff a/backend/src/data/GuildMutes.ts b/backend/src/data/GuildMutes.ts	(rejected hunks)
@@ -38 +38,6 @@ export class GuildMutes extends BaseGuildRepository {
-    const expiresAt = expiryTime ? moment.utc().add(expiryTime, "ms").format("YYYY-MM-DD HH:mm:ss") : null;
+    const expiresAt = expiryTime
+      ? moment
+          .utc()
+          .add(expiryTime, "ms")
+          .format("YYYY-MM-DD HH:mm:ss")
+      : null;
@@ -51 +56,6 @@ export class GuildMutes extends BaseGuildRepository {
-    const expiresAt = newExpiryTime ? moment.utc().add(newExpiryTime, "ms").format("YYYY-MM-DD HH:mm:ss") : null;
+    const expiresAt = newExpiryTime
+      ? moment
+          .utc()
+          .add(newExpiryTime, "ms")
+          .format("YYYY-MM-DD HH:mm:ss")
+      : null;
@@ -82 +92 @@ export class GuildMutes extends BaseGuildRepository {
-        new Brackets((qb) => {
+        new Brackets(qb => {
