diff a/backend/src/data/Configs.ts b/backend/src/data/Configs.ts	(rejected hunks)
@@ -44 +44,5 @@ export class Configs extends BaseRepository {
-    return this.configs.createQueryBuilder().where("id > :id", { id }).andWhere("is_active = 1").getMany();
+    return this.configs
+      .createQueryBuilder()
+      .where("id > :id", { id })
+      .andWhere("is_active = 1")
+      .getMany();
@@ -64 +68 @@ export class Configs extends BaseRepository {
-    return connection.transaction(async (entityManager) => {
+    return connection.transaction(async entityManager => {
