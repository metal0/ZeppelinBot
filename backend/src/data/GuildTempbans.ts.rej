diff a/backend/src/data/GuildTempbans.ts b/backend/src/data/GuildTempbans.ts	(rejected hunks)
@@ -33 +33,4 @@ export class GuildTempbans extends BaseGuildRepository {
-    const expiresAt = moment.utc().add(expiryTime, "ms").format("YYYY-MM-DD HH:mm:ss");
+    const expiresAt = moment
+      .utc()
+      .add(expiryTime, "ms")
+      .format("YYYY-MM-DD HH:mm:ss");
@@ -47 +50,4 @@ export class GuildTempbans extends BaseGuildRepository {
-    const expiresAt = moment.utc().add(newExpiryTime, "ms").format("YYYY-MM-DD HH:mm:ss");
+    const expiresAt = moment
+      .utc()
+      .add(newExpiryTime, "ms")
+      .format("YYYY-MM-DD HH:mm:ss");
