diff a/backend/src/plugins/Logs/util/getLogMessage.ts b/backend/src/plugins/Logs/util/getLogMessage.ts	(rejected hunks)
@@ -62 +62 @@ export async function getLogMessage<TLogType extends keyof ILogTypeData>(
-        (v) => v instanceof TemplateSafeUser || v instanceof TemplateSafeMember,
+        v => v instanceof TemplateSafeUser || v instanceof TemplateSafeMember,
@@ -85 +85 @@ export async function getLogMessage<TLogType extends keyof ILogTypeData>(
-            memberRoles: member ? member.roles.map((r) => r.id) : [],
+            memberRoles: member ? member.roles.map(r => r.id) : [],
@@ -99 +99 @@ export async function getLogMessage<TLogType extends keyof ILogTypeData>(
-    channelMention: (channel) => {
+    channelMention: channel => {
@@ -111 +111 @@ export async function getLogMessage<TLogType extends keyof ILogTypeData>(
-    values.tmplEval = (str) => {
+    values.tmplEval = str => {
@@ -116 +116 @@ export async function getLogMessage<TLogType extends keyof ILogTypeData>(
-  const renderLogString = (str) => renderTemplate(str, values);
+  const renderLogString = str => renderTemplate(str, values);
