diff a/backend/src/plugins/Logs/util/onMessageUpdate.ts b/backend/src/plugins/Logs/util/onMessageUpdate.ts	(rejected hunks)
@@ -20,2 +20,2 @@ export async function onMessageUpdate(
-    .map((e) => cloneDeep(e))
-    .filter((e) => (e as MessageEmbed).type === "rich");
+    .map(e => cloneDeep(e))
+    .filter(e => (e as MessageEmbed).type === "rich");
@@ -24,2 +24,2 @@ export async function onMessageUpdate(
-    .map((e) => cloneDeep(e))
-    .filter((e) => (e as MessageEmbed).type === "rich");
+    .map(e => cloneDeep(e))
+    .filter(e => (e as MessageEmbed).type === "rich");
