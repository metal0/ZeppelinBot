diff a/backend/src/plugins/Starboard/events/StarboardReactionAddEvt.ts b/backend/src/plugins/Starboard/events/StarboardReactionAddEvt.ts	(rejected hunks)
@@ -40 +40 @@ export const StarboardReactionAddEvt = starboardEvt({
-      .filter((board) => board.enabled)
+      .filter(board => board.enabled)
@@ -42 +42 @@ export const StarboardReactionAddEvt = starboardEvt({
-      .filter((board) => board.channel_id !== msg.channel.id)
+      .filter(board => board.channel_id !== msg.channel.id)
@@ -44 +44 @@ export const StarboardReactionAddEvt = starboardEvt({
-      .filter((board) => {
+      .filter(board => {
