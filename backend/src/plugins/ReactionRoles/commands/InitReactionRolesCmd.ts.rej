diff a/backend/src/plugins/ReactionRoles/commands/InitReactionRolesCmd.ts b/backend/src/plugins/ReactionRoles/commands/InitReactionRolesCmd.ts	(rejected hunks)
@@ -61,9 +61,11 @@ export const InitReactionRolesCmd = reactionRolesCmd({
-      .map((v) => v.split(/[\s=,]+/).map((v) => v.trim())) // tslint:disable-line
-      .map((pair): TReactionRolePair => {
-        const customEmojiMatch = pair[0].match(/^<a?:(.*?):(\d+)>$/);
-        if (customEmojiMatch) {
-          return [customEmojiMatch[2], pair[1], customEmojiMatch[1]];
-        } else {
-          return pair as TReactionRolePair;
-        }
-      });
+      .map(v => v.split(/[\s=,]+/).map(v => v.trim())) // tslint:disable-line
+      .map(
+        (pair): TReactionRolePair => {
+          const customEmojiMatch = pair[0].match(/^<a?:(.*?):(\d+)>$/);
+          if (customEmojiMatch) {
+            return [customEmojiMatch[2], pair[1], customEmojiMatch[1]];
+          } else {
+            return pair as TReactionRolePair;
+          }
+        },
+      );
