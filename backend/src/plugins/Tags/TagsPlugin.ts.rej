diff a/backend/src/plugins/Tags/TagsPlugin.ts b/backend/src/plugins/Tags/TagsPlugin.ts	(rejected hunks)
@@ -132 +132 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-    state.onMessageCreateFn = (msg) => onMessageCreate(pluginData, msg);
+    state.onMessageCreateFn = msg => onMessageCreate(pluginData, msg);
@@ -135 +135 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-    state.onMessageDeleteFn = (msg) => onMessageDelete(pluginData, msg);
+    state.onMessageDeleteFn = msg => onMessageDelete(pluginData, msg);
@@ -185 +185,4 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-        return moment.utc(reference, "x").add(delayMS).valueOf();
+        return moment
+          .utc(reference, "x")
+          .add(delayMS)
+          .valueOf();
@@ -204 +207,4 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-        return moment.utc(reference, "x").subtract(delayMS).valueOf();
+        return moment
+          .utc(reference, "x")
+          .subtract(delayMS)
+          .valueOf();
@@ -222 +228 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-      mention: (input) => {
+      mention: input => {
@@ -245 +251 @@ export const TagsPlugin = zeppelinGuildPlugin<TagsPluginType>()({
-      isMention: (input) => {
+      isMention: input => {
