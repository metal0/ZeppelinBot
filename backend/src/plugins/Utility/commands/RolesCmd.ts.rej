diff a/backend/src/plugins/Utility/commands/RolesCmd.ts b/backend/src/plugins/Utility/commands/RolesCmd.ts	(rejected hunks)
@@ -31 +31 @@ export const RolesCmd = utilityCmd({
-      roles = roles.filter((r) => r.name.toLowerCase().includes(searchStr) || r.id === searchStr);
+      roles = roles.filter(r => r.name.toLowerCase().includes(searchStr) || r.id === searchStr);
@@ -78 +78 @@ export const RolesCmd = utilityCmd({
-      roles.sort(sorter((r) => r.name.toLowerCase(), sortDir));
+      roles.sort(sorter(r => r.name.toLowerCase(), sortDir));
@@ -88 +88 @@ export const RolesCmd = utilityCmd({
-      const roleLines = chunk.map((role) => {
+      const roleLines = chunk.map(role => {
