diff a/backend/src/plugins/Utility/functions/getRoleInfoEmbed.ts b/backend/src/plugins/Utility/functions/getRoleInfoEmbed.ts	(rejected hunks)
@@ -38 +38 @@ export async function getRoleInfoEmbed(
-  const rolePerms = Object.keys(role.permissions.toJSON()).map((p) =>
+  const rolePerms = Object.keys(role.permissions.toJSON()).map(p =>
@@ -44 +44 @@ export async function getRoleInfoEmbed(
-      .replace(/(^\w{1})|(\s{1}\w{1})/g, (l) => l.toUpperCase()),
+      .replace(/(^\w{1})|(\s{1}\w{1})/g, l => l.toUpperCase()),
@@ -57 +57,4 @@ export async function getRoleInfoEmbed(
-      Color: **#${role.color.toString(16).toUpperCase().padStart(6, "0")}**
+      Color: **#${role.color
+        .toString(16)
+        .toUpperCase()
+        .padStart(6, "0")}**
