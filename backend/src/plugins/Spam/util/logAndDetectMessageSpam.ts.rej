diff a/backend/src/plugins/Spam/util/logAndDetectMessageSpam.ts b/backend/src/plugins/Spam/util/logAndDetectMessageSpam.ts	(rejected hunks)
@@ -106,2 +106,2 @@ export async function logAndDetectMessageSpam(
-        const savedMessages = recentActions.map((a) => a.extraData as SavedMessage);
-        const msgIds = savedMessages.map((m) => m.id);
+        const savedMessages = recentActions.map(a => a.extraData as SavedMessage);
+        const msgIds = savedMessages.map(m => m.id);
@@ -115 +115 @@ export async function logAndDetectMessageSpam(
-        additionalMessages.forEach((m) => msgIds.push(m.id));
+        additionalMessages.forEach(m => msgIds.push(m.id));
@@ -119 +119 @@ export async function logAndDetectMessageSpam(
-          msgIds.forEach((id) => pluginData.state.logs.ignoreLog(LogType.MESSAGE_DELETE, id));
+          msgIds.forEach(id => pluginData.state.logs.ignoreLog(LogType.MESSAGE_DELETE, id));
@@ -129 +129 @@ export async function logAndDetectMessageSpam(
-          .map((m) => m.id)
+          .map(m => m.id)
@@ -191 +191 @@ export async function logAndDetectMessageSpam(
-    (err) => {
+    err => {
