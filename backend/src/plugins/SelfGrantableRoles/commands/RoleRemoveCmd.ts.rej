diff a/backend/src/plugins/SelfGrantableRoles/commands/RoleRemoveCmd.ts b/backend/src/plugins/SelfGrantableRoles/commands/RoleRemoveCmd.ts	(rejected hunks)
@@ -32 +32 @@ export const RoleRemoveCmd = selfGrantableRolesCmd({
-      (id) => pluginData.guild.roles.cache.get(id as Snowflake)!,
+      id => pluginData.guild.roles.cache.get(id as Snowflake)!,
@@ -34 +34 @@ export const RoleRemoveCmd = selfGrantableRolesCmd({
-    const roleIdsToRemove = rolesToRemove.map((r) => r.id);
+    const roleIdsToRemove = rolesToRemove.map(r => r.id);
@@ -38 +38 @@ export const RoleRemoveCmd = selfGrantableRolesCmd({
-      const newRoleIds = msg.member.roles.cache.filter((role) => !roleIdsToRemove.includes(role.id));
+      const newRoleIds = msg.member.roles.cache.filter(role => !roleIdsToRemove.includes(role.id));
@@ -45 +45 @@ export const RoleRemoveCmd = selfGrantableRolesCmd({
-        const removedRolesStr = rolesToRemove.map((r) => `**${r.name}**`);
+        const removedRolesStr = rolesToRemove.map(r => `**${r.name}**`);
