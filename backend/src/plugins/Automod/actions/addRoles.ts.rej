diff a/backend/src/plugins/Automod/actions/addRoles.ts b/backend/src/plugins/Automod/actions/addRoles.ts	(rejected hunks)
@@ -19 +19 @@ export const AddRolesAction = automodAction({
-    const members = unique(contexts.map((c) => c.member).filter(nonNullish));
+    const members = unique(contexts.map(c => c.member).filter(nonNullish));
@@ -43 +43 @@ export const AddRolesAction = automodAction({
-        (roleId) => pluginData.guild.roles.cache.get(roleId as Snowflake)?.name || roleId,
+        roleId => pluginData.guild.roles.cache.get(roleId as Snowflake)?.name || roleId,
@@ -55 +55 @@ export const AddRolesAction = automodAction({
-      members.map(async (member) => {
+      members.map(async member => {
