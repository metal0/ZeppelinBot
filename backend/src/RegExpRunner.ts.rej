diff a/backend/src/RegExpRunner.ts b/backend/src/RegExpRunner.ts	(rejected hunks)
@@ -30 +30 @@ const FINAL_REGEX_TIMEOUT = 5 * SECONDS;
-const regexTimeoutUpgradePromise = new Promise((resolve) => setTimeout(resolve, INITIAL_REGEX_TIMEOUT_DURATION));
+const regexTimeoutUpgradePromise = new Promise(resolve => setTimeout(resolve, INITIAL_REGEX_TIMEOUT_DURATION));
