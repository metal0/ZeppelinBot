diff a/backend/src/utils/templateSafeObjects.ts b/backend/src/utils/templateSafeObjects.ts	(rejected hunks)
@@ -265 +265 @@ export function memberToTemplateSafeMember(member: GuildMember | PartialGuildMem
-    roles: [...member.roles.cache.mapValues((r) => roleToTemplateSafeRole(r)).values()],
+    roles: [...member.roles.cache.mapValues(r => roleToTemplateSafeRole(r)).values()],
@@ -325 +325 @@ export function savedMessageToTemplateSafeSavedMessage(savedMessage: SavedMessag
-        (att) =>
+        att =>
@@ -346 +346 @@ export function savedMessageToTemplateSafeSavedMessage(savedMessage: SavedMessag
-        (embed) =>
+        embed =>
@@ -355 +355 @@ export function savedMessageToTemplateSafeSavedMessage(savedMessage: SavedMessag
-              (field) =>
+              field =>
@@ -410 +410 @@ export function savedMessageToTemplateSafeSavedMessage(savedMessage: SavedMessag
-        (sticker) =>
+        sticker =>
@@ -462 +462 @@ export function getTemplateSafeMemberLevel(pluginData: GuildPluginData<any>, mem
-    if (member.id === id || member.roles?.find((r) => r.id === id)) {
+    if (member.id === id || member.roles?.find(r => r.id === id)) {
